<h1>Co se stalo nového</h1>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @actualities.each do |actuality| %>
      <tr>
        <td><%= actuality.title %></td>
        <td><%= actuality.body %></td>
        <td><%= link_to 'Show', actuality %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

